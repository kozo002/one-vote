
import { choiceExample } from 'one-vote/helpers/choice-example';
import { module, test } from 'qunit';

module('Unit | Helper | choice example');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = choiceExample([42]);
  assert.ok(result);
});

